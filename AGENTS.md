# Notes for AI Agents

Follow README section 快速开始（AI Agent）.

```bash
git clone https://github.com/Doki21yy/yingzao-cli.git && cd yingzao-cli && ./scripts/install.sh --yes
source .yingzao-env && yingzao doctor
```
